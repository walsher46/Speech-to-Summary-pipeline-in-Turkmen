# Speech-to-Summary-pipeline-in-Turkmen
The developed system constitutes a two-stage pipeline for Turkmen speech and summary. In the first stage, the audio signal (WAV/MP3, 16 kHz) is fed into the ASR model. In the second stage, the text is tokenized and fed into the summarization model, which generates a concise summary of up to 128 tokens. 
